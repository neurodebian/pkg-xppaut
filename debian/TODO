* fix up the left over patches (may be could just be dropped)

* inflitrate source tarballs:

 - remove backup files *~
 - remove *.jar?
 - built materials install.pdf

* add Git- fields to debian/control

* at the end we aim to achieve lintian-free package

 run recent lintian *.changes

* git-buildpackage

  git-pbuilder create

  git-buildpackage --git-pbuilder
